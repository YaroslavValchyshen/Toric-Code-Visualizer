name: Build and Deploy WebAssembly App

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Emscripten Toolchain
      uses: mymindstorm/setup-emsdk@v14
      with:
        version: 'latest'

    - name: Configure Project with CMake
      run: |
        emcmake cmake -B build \
          -DCMAKE_EXE_LINKER_FLAGS="-s FULL_ES3=1 --preload-file Shaders --shell-file ../shell_minimal.html"

    - name: Build Project
      run: cmake --build build

    - name: Prepare Artifacts for Deployment
      run: |
        mkdir -p dist
        cp build/ToricCode.html dist/index.html
        cp build/ToricCode.js dist/
        cp build/ToricCode.wasm dist/
        cp build/ToricCode.data dist/ 2>/dev/null || true

    - name: Push Build to GitHub Pages Branch
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        folder: dist
        branch: gh-pages
